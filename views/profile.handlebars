
    <h4>Profile</h4>
    <div id="savedArmorSets">
{{#if armors.length}}
      {{#each armors}}
      <tr class="armor-row" data-type="{{this.type}}">
        <td>{{this.name}}</td>
        <td>{{this.type}}</td>
      
      </tr>
    {{/each}}
    {{else}}
    <p>No Armor Saved</p>
    {{/if}}
</div>

<script src="/js/profile.js"></script>
